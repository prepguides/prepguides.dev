{
  "name": "prepguides-minimal",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "python3 -m http.server 3000",
    "build": "echo 'Static build - no build step needed'",
    "start": "python3 -m http.server 3000"
  }
}